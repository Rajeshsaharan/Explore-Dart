void main() {
  //anything we want to do
  printanything("hello");

  returnage(23); //will return int age 
}

// how to define function

// what return has put just before Function
void printanything(dynamic variable) {
  print(variable); // now we can use in our main function
}

int returnage(int age) {
  return age;
}
